{{- if .Values.networkPolicy.enabled }}
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "safe-infrastructure.fullname" . }}
  labels:
    {{- include "safe-infrastructure.labels" . | nindent 4 }}
spec:
  # All chart pods carry the shared selectorLabels (app.kubernetes.io/name
  # and app.kubernetes.io/instance) regardless of component, so this single
  # policy targets every workload in the release.
  podSelector:
    matchLabels:
      {{- include "safe-infrastructure.selectorLabels" . | nindent 6 }}
  policyTypes:
    - Ingress
    - Egress
  {{- with .Values.networkPolicy.ingress }}
  ingress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  {{- with .Values.networkPolicy.egress }}
  egress:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
