{{- /*
  This file intentionally left empty.

  MINIO_ROOT_USER and MINIO_ROOT_PASSWORD are generated alongside
  STORAGE_CONFIG inside templates/secret.yaml so the two cannot drift
  on first install. The bundled MinIO deployment reads them from the
  main chart-managed Secret.
*/ -}}
