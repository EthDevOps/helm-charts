apiVersion: apps/v1
kind: Deployment
metadata:
  name: rekoni
  labels:
    {{- include "huly.labels" . | nindent 4 }}
    app: rekoni
  annotations:
    # Upstream hardcoreeng/rekoni-service image bakes in a low uid.
    kube-score/ignore: {{ include "huly.kubeScoreIgnore" . }}
spec:
  replicas: {{ .Values.rekoni.replicas }}
  selector:
    matchLabels:
      app: rekoni
  template:
    metadata:
      annotations:
        {{- include "huly.checksumAnnotations" . | nindent 8 }}
      labels:
        {{- include "huly.labels" . | nindent 8 }}
        app: rekoni
    spec:
      {{- include "huly.scheduling" . | nindent 6 }}
      containers:
        - name: rekoni
          image: {{ .Values.hulyRegistry }}/rekoni-service:{{ .Values.hulyVersion }}
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 4004
          env:
            {{- include "huly.envSecret" (dict "name" "SECRET" "key" "SERVER_SECRET" "root" .) | nindent 12 }}
          # Single readinessProbe; identical TCP livenessProbe omitted.
          readinessProbe:
            tcpSocket:
              port: 4004
            initialDelaySeconds: 5
            periodSeconds: 10
          {{- with .Values.rekoni.resources }}
          resources:
            {{- toYaml . | nindent 12 }}
          {{- end }}
