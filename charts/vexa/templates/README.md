# vexa chart · templates

Kubernetes manifests rendered by the `vexa` Helm chart — one `deployment-*.yaml` per service (gateway, admin-api, meeting-api, agent-api, terminal, runtime, redis, minio, pgbouncer, …) plus supporting resources. Values come from the chart `values.yaml`.
