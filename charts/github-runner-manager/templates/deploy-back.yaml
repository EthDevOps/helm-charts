apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-github-autoscaler
  labels:
    app: {{ .Release.Name }}-github-autoscaler
spec:
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: {{ .Release.Name }}-github-autoscaler
  template:
    metadata:
      annotations:
        prometheus.io/scrape: 'true'
        prometheus.io/path: '/metrics'
        prometheus.io/port: '9000'
      labels:
        app: {{ .Release.Name }}-github-autoscaler
    spec:
      containers:
        - name: github-autoscaler
          image: {{ .Values.imageName }}:{{ .Values.imageTag }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          ports:
            - name: http
              containerPort: 8080 
            - name: metrics
              containerPort: 9000 
          env:
          - name: CONFIG_DIR
            value: "/data/config"
          {{- if .Values.observability.otelExporterEndpoint }}
          - name: OTEL_EXPORTER_OTLP_ENDPOINT
            value: {{ .Values.observability.otelExporterEndpoint | quote }}
          - name: OTEL_SERVICE_NAME
            value: "github-actions-orchestrator"
          {{- if or .Values.observability.otelAuth.user .Values.observability.otelAuth.existingSecret }}
          - name: OTEL_EXPORTER_OTLP_AUTH_USER
            {{- if .Values.observability.otelAuth.existingSecret }}
            valueFrom:
              secretKeyRef:
                name: {{ .Values.observability.otelAuth.existingSecret }}
                key: {{ .Values.observability.otelAuth.userKey }}
            {{- else }}
            value: {{ .Values.observability.otelAuth.user | quote }}
            {{- end }}
          {{- end }}
          {{- if or .Values.observability.otelAuth.password .Values.observability.otelAuth.existingSecret }}
          - name: OTEL_EXPORTER_OTLP_AUTH_PASSWORD
            {{- if .Values.observability.otelAuth.existingSecret }}
            valueFrom:
              secretKeyRef:
                name: {{ .Values.observability.otelAuth.existingSecret }}
                key: {{ .Values.observability.otelAuth.passwordKey }}
            {{- else }}
            value: {{ .Values.observability.otelAuth.password | quote }}
            {{- end }}
          {{- end }}
          {{- end }}
          {{- if .Values.observability.pyroscopeServerAddress }}
          - name: PYROSCOPE_SERVER_ADDRESS
            value: {{ .Values.observability.pyroscopeServerAddress | quote }}
          - name: PYROSCOPE_APPLICATION_NAME
            value: "github-actions-orchestrator"
          {{- if .Values.observability.pyroscopeLogLevel }}
          - name: PYROSCOPE_LOG_LEVEL
            value: {{ .Values.observability.pyroscopeLogLevel | quote }}
          {{- end }}
          {{- if or .Values.observability.pyroscopeAuth.user .Values.observability.pyroscopeAuth.existingSecret }}
          - name: PYROSCOPE_BASIC_AUTH_USER
            {{- if .Values.observability.pyroscopeAuth.existingSecret }}
            valueFrom:
              secretKeyRef:
                name: {{ .Values.observability.pyroscopeAuth.existingSecret }}
                key: {{ .Values.observability.pyroscopeAuth.userKey }}
            {{- else }}
            value: {{ .Values.observability.pyroscopeAuth.user | quote }}
            {{- end }}
          {{- end }}
          {{- if or .Values.observability.pyroscopeAuth.password .Values.observability.pyroscopeAuth.existingSecret }}
          - name: PYROSCOPE_BASIC_AUTH_PASSWORD
            {{- if .Values.observability.pyroscopeAuth.existingSecret }}
            valueFrom:
              secretKeyRef:
                name: {{ .Values.observability.pyroscopeAuth.existingSecret }}
                key: {{ .Values.observability.pyroscopeAuth.passwordKey }}
            {{- else }}
            value: {{ .Values.observability.pyroscopeAuth.password | quote }}
            {{- end }}
          {{- end }}
          {{- end }}
          envFrom:
          - secretRef:
              name: {{ .Values.envFromSecret }}
          volumeMounts:
            - name: config
              mountPath: /data/config
              readOnly: true
      nodeSelector:
        kubernetes.io/os: linux
      volumes:
        - name: config
          secret:
            secretName: {{ .Release.Name }}-config
