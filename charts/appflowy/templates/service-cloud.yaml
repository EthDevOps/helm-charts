apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-cloud-app
  {{- with .Values.cloud.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.cloud.service.type | default "ClusterIP" }}
  ports:
    - name: http
      port: {{ .Values.cloud.service.port }}
      targetPort: 8000
      {{- if and (eq .Values.cloud.service.type "NodePort") .Values.cloud.service.nodePort }}
      nodePort: {{ .Values.cloud.service.nodePort }}
      {{- end }}
  selector:
    app: {{ .Release.Name }}-cloud
